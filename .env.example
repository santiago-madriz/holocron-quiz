# Public deploy-time configuration only.
# GA4 measurement IDs are not secrets, but they should still be injected by deploy tooling
# instead of edited directly into committed source files.
GA_MEASUREMENT_ID=
